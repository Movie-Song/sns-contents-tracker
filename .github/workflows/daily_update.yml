name: Daily Content Update
on:
  schedule:
    # 의미: UTC 15:00 = 한국시간 자정(00:00)
    - cron: "0 15 * * *"
  
  # 수동 실행 가능
  workflow_dispatch:

jobs:
  update-content:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4
      
      - name: 🐍 Python 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 의존성 설치
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 🚀 콘텐츠 수집 실행
        run: python main.py
        env:
          NOTION_API_KEY: ${{ secrets.NOTION_API_KEY }}
          DATABASE_ID: ${{ secrets.DATABASE_ID }}
          NOTION_DATABASE_ID: ${{ secrets.DATABASE_ID }}  # 👈 main.py가 이 이름을 사용
          TISTORY_BLOGS: ${{ secrets.TISTORY_BLOGS }}     # 👈 새로 추가!
          TWITTER_USERNAME: ${{ secrets.TWITTER_USERNAME }} # 👈 새로 추가!
      
      - name: ✅ 완료
        run: echo "콘텐츠 업데이트가 완료되었습니다!"
